DATAS SEGMENT
    ;此处输入数据段代码
    BUFFERS DB 'ABCDEFGH'
    BUFFER DB 50 DUP(?)
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    ;此处输入代码段代码
    MOV CX,8
    LEA SI,BUFFERS
    LEA DI,BUFFER
    ADD SI,CX
    DEC SI
L:
	MOV AL,[SI]
	MOV [DI],AL
	INC DI
	DEC SI
	LOOP L
    
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START

