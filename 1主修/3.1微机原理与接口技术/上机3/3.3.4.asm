DATAS SEGMENT
	STR1 DB 'ASSEMBLE LANGUAGE'
	STR2 DB 20 DUP(?)
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    MOV CX,17
    LEA SI,STR1
    MOV AL,20H
    
L1:
	CMP [SI],AL
	JZ L2
	INC SI
	LOOP L1
	
L2:
    ;此处输入代码段代码
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
