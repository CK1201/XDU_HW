DATAS SEGMENT
	STR1 DB 'ASSEMBLE LANGUAGE'
	STR2 DB 20 DUP(?)
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    LEA SI,STR1
    MOV DX,[SI+6]
    MOV AH,2
	INT 21H
	
	XCHG DL,DH
	MOV AH,2
	INT 21H
    
    
    ;此处输入代码段代码
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
